# Flatpak

- flatpak install "app name"

        flatpak install discord

- flatpak remove "app name"

        flatpak remove discord

- flatpak update

- flatpak uninstall --unused (to clean up unused runtimes)

- flatpak list
- flatpak search
  ```bash
  flatpak search discord
  ```
- flatpak repair
